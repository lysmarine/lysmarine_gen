---
version: 2.1

orbs:
  cloudsmith: cloudsmith/cloudsmith@1.0.4

executors:
  linux:
    machine:
      image: ubuntu-2004:202010-01
    resource_class: medium

commands:
  prepare-linux-machine:
    description: "Install the requirements to run lysmarine build script"
    steps:
      - checkout
      - run: sudo apt-get -q update
      - run: sudo apt-get -y -q install proot qemu qemu-user git live-build p7zip-full
      - run: cd cross-build-release; sudo chmod -v u+w *.sh;

jobs:
  build-raspios-arm32:
    executor: linux
    environment:
      - PKG_DISTRO=buster
      - LYSMARINE_VER=0.9.0.10
      - PKG_RELEASE=raspios
      - PKG_ARCH=armhf
    steps:
      - prepare-linux-machine
      - run:
          command: cd cross-build-release;mkdir -p release/armhf ; touch release/armhf/foo.txt ; echo boo >> release/armhf/foo.txt
          no_output_timeout: 30m
      - store_artifacts:
          path: ./cross-build-release/release/armhf/
          destination: release/raspios-armhf/

  build-raspios-arm64:
    executor: linux
    environment:
      - PKG_DISTRO=buster
      - LYSMARINE_VER=0.9.0.10
      - PKG_RELEASE=raspios
      - PKG_ARCH=arm64
    steps:
      - prepare-linux-machine
      - run:
          command: cd cross-build-release; sudo /bin/bash -xe ./raspbian.sh $PKG_ARCH $LYSMARINE_VER
          no_output_timeout: 30m
      - store_artifacts:
          path: ./cross-build-release/release/$PKG_ARCH/*
          destination: release/$PKG_ARCH/

  build-armbian-pine64so:
    executor: linux
    environment:
      - PKG_DISTRO=buster
      - LYSMARINE_VER=0.9.0.10
      - PKG_RELEASE=armbian-pineA64
      - PKG_ARCH=armbian-pineA64
    steps:
      - prepare-linux-machine
      - run:
          command: cd cross-build-release; sudo /bin/bash -xe ./armbian-pine64so.sh $PKG_ARCH $LYSMARINE_VER
          no_output_timeout: 30m
      - store_artifacts:
          path: ./cross-build-release/release/$PKG_ARCH/*
          destination: release/$PKG_ARCH/

  build-debian-live:
    executor: linux
    environment:
      - PKG_DISTRO=buster
      - LYSMARINE_VER=0.9.0.10
      - PKG_ARCH=amd64
    steps:
      - prepare-linux-machine
      - run: cd cross-build-release; sudo /bin/bash -xe ./debian-live.sh
      - store_artifacts:
          path: ./cross-build-release/release/$PKG_ARCH/*
          destination: release/$PKG_ARCH/

  publish:
    executor: cloudsmith/default
    steps:
      - checkout
      - cloudsmith/ensure-api-key
      - cloudsmith/install-cli
      - run: .circleci/publish-cloudsmith.sh img lysmarineos/images ${PKG_DISTRO}/${PKG_RELEASE}

workflows:
  version: 2
  build_all:
    jobs:
      - build-raspios-arm32

      - publish:
          requires:
            - build-raspios-arm32

